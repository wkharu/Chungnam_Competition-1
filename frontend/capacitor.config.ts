import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'kr.chungnam.tteonago',
  appName: '\uB5A0\uB098GO',
  webDir: 'dist',
  server: {
    cleartext: true
  }
};

export default config;
